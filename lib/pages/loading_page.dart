import 'package:flutter/material.dart';

class LoadingnPage extends StatelessWidget {
  const LoadingnPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('LoginPage'),
      ),
    );
  }
}